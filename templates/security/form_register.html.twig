{% extends "base.html.twig" %}

{% block title %}Inscription{% endblock %}

{% block body %}

<h1 class="text-center">Inscription</h1>

{# {{ form(form)}} #}

    <div class="row m-4 d-flex justify-content-center">
        <div class="col-9 p-4 mx-auto border border-secondary">
            {{ form_start(form) }}
            <div class="row d-flex justify-content-center">
                <div class="col-4"> {{ form_row(form.email) }} </div>
                <div class="col-4"> {{ form_row(form.password) }} </div>
            </div>
            <div class="row mt-3 d-flex justify-content-center">
                <div class="col-4"> {{ form_row(form.firstname) }} </div>
                <div class="col-4"> {{ form_row(form.lastname) }} </div>
            </div>
            <div class="row mt-3 d-flex justify-content-center">
                <div class="col-4"> {{ form_row(form.gender) }} </div>
                <div class="col-4"> {{ form_row(form.submit) }} </div>
            </div>
            {# <div class="row mt-3 d-flex justify-content-center"> #}
                {# <button type="submit" class="d-block col-3 btn btn-success">Valider le formulaire</button> #}
                {# <div class="col-3"> {{ form_row(form.submit) }} </div> #}
            {# </div> #}
        
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}